// cach thuat toan sap xep insertionSort hoat dong:
// - chen phan tu nho hon len phia truoc

#include <bits/stdc++.h>
using namespace std;

void InsertionSort(vector<int>& a,int n){
    for(int i = 1; i < n; i++){
        int key = a[i];
        int pos = i - 1;
        while(pos >= 0 && a[pos] > key){
            a[pos+1] = a[pos];
            pos--;
        }
        a[pos + 1] = key;
    }
}

int main() {
    int n; cin >> n;
    vector<int> a(n);
    for(int i = 0; i < n; i++){
        cin >> a[i];
    }
    InsertionSort(a,n);
    for(int i = 0; i < n; i++){
        cout << a[i] << " ";
    }
    system("pause");
    return 0;
}
